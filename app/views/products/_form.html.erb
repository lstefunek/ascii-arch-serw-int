<%= simple_form_for(@product) do |f| %>
	<%= f.input :name %>
	<%= f.input :body, :input_html => { :cols => 57, :rows => 8} %>
	<%= f.input :price, :hint => "prices should be in PLN" %>
	<%= f.input :count %>
	<%= f.button :submit %>
<% end %>
